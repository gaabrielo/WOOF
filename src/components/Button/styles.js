import styled from 'styled-components';

export const Container = styled.button`
  width: 100%;
  height: 2.5rem;
  background: var(--text);
  text-transform: uppercase;
  border: 0;
  color: var(--white);
  font-size: 0.9rem;
  border-radius: 3px;
`;
